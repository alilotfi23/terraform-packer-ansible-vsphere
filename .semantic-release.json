{
  "branches": ["main"],
  "analyzeCommits": ["feat", "fix", "breaking change"],
  "release": {
    "tagFormat": "${{ env.GITHUB_REF_SLUG }}-{version}"
  },
  "plugins": ["@semantic-release/changelog", "@semantic-release/git"]
}
